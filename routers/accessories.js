console.log('from accessories');

const express = require('express');
const router  = express.Router();

const app = express();


router.get('', (req, res) => {
});
router.get('/', (req, res) => {
});

module.exports = router;